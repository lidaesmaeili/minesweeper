import React,{useState,useEffect,FunctionComponent} from 'react';
export interface ITimerParam{
    isTimerStarted:boolean
}
export const Timer:FunctionComponent<ITimerParam> = (params)=>{
    return <h1></h1>
}
